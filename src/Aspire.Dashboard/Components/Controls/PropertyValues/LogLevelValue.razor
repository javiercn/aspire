@using Aspire.Dashboard.Utils

@if (LogEntry.IsError)
{
    <FluentIcon Icon="Octicons.Size16.ErrorCircle" Color="Color.Error" Class="severity-icon" />
}
else if (LogEntry.IsWarning)
{
    <FluentIcon Icon="Octicons.Size16.Warning" Color="Color.Warning" Class="severity-icon" />
}

<FluentHighlighter HighlightedText="@HighlightText" Text="@Value" />
